/// <reference types="next" />
/// <reference types="next/image-types/global" />

// Diese Datei wird von Next.js normalerweise selbst beim ersten Start
// erzeugt. Explizit angelegt, damit "tsc --noEmit" auch dann funktioniert,
// wenn es vor dem ersten "next dev"/"next build" laeuft (z. B. in der CI).
